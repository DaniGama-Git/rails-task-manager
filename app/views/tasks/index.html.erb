<h1>Task List - Whoop Whoop</h1>
<ul>
  <ul>
    <% @tasks.each do |task| %>
      <%= link_to task.title, task_path(task) %>
    <% end %>
  </ul>

  <h2>Create New Task</h2>
  <%= link_to "Create New Task" , new_path %>
